To use these macros you only need to drag to FIJI main menu, change the root and the plate names and dates.

You can also open several FIJI windows paralerly using the fiji.sh script (bash.scripts) and assign a fraction of the memory to each window. By doing this you can divide 
the analysis by selecting a proportional number of files replacing "list1.length" by a number.
